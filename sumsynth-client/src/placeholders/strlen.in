fn strlen(s: ^char) := {
    locals: (n: int32, ret: int32)
    pre: str(s; n)
    post: ret == n
}

pred str(s: ^char; n: int32) := {
    s -> '\0' <*> n == 0;
    exists [c: char, k: int32] s -> c <*> c != '\0' <*> n == k + 1 <*> str(s + 1; k)
}